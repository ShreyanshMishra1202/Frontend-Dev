{
    var a=10;
    let b=20;
    const c=30;
}

console.log("a: " + a);
// console.log("b: " + b); // ReferenceError: b is not defined  (cannot access let variable outside block (block-scoped) )
// console.log("c: " + c); // ReferenceError: c is not defined  (cannot access let variable outside block (block-scoped) )